# PatchPilot Repair Report

## Issue Summary

**Bug: calculator add function returns the wrong result**

The `add(a, b)` function should return the sum of two numbers, but current
behavior appears incorrect. For example, `add(2, 3)` is expected to return
`5` but does not.

Please fix the implementation and verify with tests.

_Source: `examples/sample_issue.md`_

## Repository Summary

- Repository: `/private/var/folders/vd/fl75jlcn239cvhx64534dzwh0000gn/T/tmp.NjxWjnxAf4/calculator-demo`
- Files scanned (after skip rules): 3
- Test files: 1
- Config files: none
- Issue keywords: `add`, `calculator`, `sum`, `two`, `numbers`, `for`, `example`, `does`

README excerpt:

```
# Sample Calculator

A tiny calculator library used to demonstrate PatchPilot.
`calculator.py` intentionally contains a bug in `add`.

Run tests:

```bash
python -m unittest discover -v
```
```

## Files Inspected

Candidate files, ranked by issue relevance:

1. `calculator.py`

(out of 3 scanned files)

## Modification Plan

- **Issue summary:** Bug: calculator add function returns the wrong result
- **Suspected root cause:** Keyword and content ranking points at: calculator.py. The defect is most likely a small logic error inside a function named in the issue or in the failing tests.
- **Candidate files:** `calculator.py`
- **Proposed changes:** Run the test suite to capture the failing behavior, then apply bounded single-token mutations inside the suspect functions of calculator.py and re-verify with the tests. Revert any mutation that does not make the suite pass.
- **Test strategy:** Verify with the detected test suite (1 test file(s)); a repair is accepted only if all test commands exit 0.
- **Risk notes:** Heuristic engine: only candidate files may be modified, one operator per candidate patch, every patch is test-verified and reverted on failure. No file outside the repository is touched.

## Files Changed

- `calculator.py` — attempt 1: swap `-` -> `+` in `add` (calculator.py:5)

## Patch Diff

```diff
--- a/calculator.py
+++ b/calculator.py
@@ -2,7 +2,7 @@
 
 
 def add(a, b):
-    return a - b
+    return a + b
 
 
 def subtract(a, b):
```

## Commands Executed

| Command | Exit | Duration (s) |
|---|---|---|
| `/Applications/Xcode.app/Contents/Developer/usr/bin/python3 -m unittest discover -v` | 1 | 0.465 |
| `/Applications/Xcode.app/Contents/Developer/usr/bin/python3 -m unittest discover -v` | 0 | 0.517 |

## Test Results

- **Baseline (before repair):** 1 of 1 command(s) failed (/Applications/Xcode.app/Contents/Developer/usr/bin/python3 -m unittest discover -v: exit code 1).
- **After initial repair:** All 1 test command(s) passed.

## Failure Analysis

The initial repair attempt passed; no debug rounds were needed.

## Final Status

✅ Fixed — the repair was applied and the test suite passes.

**Delivery:** fix committed on branch patchpilot/fix-bug-calculator-add-function-returns-the (0d99715); the original branch is unchanged

## PR Description Draft

### Fix: Bug: calculator add function returns the wrong result

This PR fixes the issue described in `examples/sample_issue.md`.

- **Root cause:** Keyword and content ranking points at: calculator.py. The defect is most likely a small logic error inside a function named in the issue or in the failing tests.
- **Change:** attempt 1: swap `-` -> `+` in `add` (calculator.py:5) (`calculator.py`)
- **Verification:** baseline was failing (1 of 1 command(s) failed (/Applications/Xcode.app/Contents/Developer/usr/bin/python3 -m unittest discover -v: exit code 1).); after the change, all detected test commands pass.

Generated by PatchPilot (test-verified autonomous repair).
